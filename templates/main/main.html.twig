{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/main.css') }}">
    <link rel="stylesheet" href="{{ asset('css/markup.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-sm-8 ">
                <div class="block">
                    <div class="block-header">
                        Здесь ты сможешь найти полезную информацию и новости сайта
                    </div>
                    <div class="block-body ml-4">
                        <p><i class="fas fa-angle-right"></i> Чтобы начать поиск компании нажми кнопку <i>[Поиск]</i> справа</p>
                        <p><i class="fas fa-angle-right"></i> Не понравилась компания или место? Ты можешь пропустить ее, нажав кнопку <i>[Дальше]</i>. Так можно сделать только 2 раза в день.</p>
                        <p><i class="fas fa-angle-right"></i> Ну вроде пока все :) И помни: <br></p>
                        <p><i>Доброта и позитивное настроение - лучший способ создать хорошее первое впечатление</i></p>

                        <div class="text-center">
                            <img src="{{ asset('img/gatsby_motivation.png') }}" class="m-3">
                        </div>

                        <blockquote class="blockquote text-right mb-0">
                            <p class="mb-0">Живешь ведь только раз! Решайся!</p>
                            <footer class="blockquote-footer">Великий Гэтсби</footer>
                        </blockquote>
                    </div>
                </div>
            </div>

            <div class="col-sm-4 position-relative">
                <div class="block">
                    <div class="block-body">
                        <div class="user-party-wrapper">
                            <div class="user-icon-block float-left">
                                <img src="{{ asset(app.user.avatarPath) }}" alt="Аватар пользователя {{ app.user.name }}">
                                <br>
                                {{ app.user.name }}
                            </div>
                            <div class="clearfix"></div>
                        </div>

                        <div class="event-party-history-wrapper">
                            <button type="button" class="btn btn-light event-party-history-btn">История</button>
                        </div>

                        <div class="search-criteria-wrapper">
                            {{ form_start(searchCriteriaForm) }}
                                <p>В какое время вы cможете пойти?</p>

                                <div class="row mb-3">
                                    <div class="col-sm-6 form-group">{{ form_widget(searchCriteriaForm.timeFrom) }}</div>
                                    <div class="col-sm-6 form-group">{{ form_widget(searchCriteriaForm.timeTo) }}</div>
                                </div>

                                <div class="text-center">
                                    <button type="submit" class="btn btn-primary search-event-party-btn">Найти</button>
                                </div>
                            {{ form_end(searchCriteriaForm) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
