<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Aloner{% endblock %}</title>
        {% block stylesheets %}
            {#<link rel="stylesheet" href="{{ asset('build/vendor.css') }}">#}
            <link rel="stylesheet" href="{{ asset('build/base.css') }}">
        {% endblock %}
    </head>
    <body>
        <header class="app-header">
            <div class="container">
                <div class="row">
                    <div class="col-sm-12 logo-wrapper">
                        <img class="logo" src="{{ asset('img/app_logo.png') }}" alt="AL">
                        <span class="logo-text">Aloner</span>

                        {% if is_granted(constant('App\\Entity\\User::ROLE_FULL_REG')) %}
                            <a class="float-right" href="{{ path('app_logout') }}">Выйти</a>
                        {% endif %}
                    </div>
                </div>
            </div>
        </header>

        <div class="app-content">
            {% block body %}{% endblock %}
        </div>

        <footer class="app-footer">
            <div class="container">
                <div class="row">
                    <div class="col-sm-12">
                        2019 &copy; Aloner | Поддержка
                    </div>
                </div>
            </div>
        </footer>

        {% block javascripts %}
            {#<script src="{{ asset('build/vendor.js') }}"></script>#}
            <script src="{{ asset('build/base.js') }}"></script>
        {% endblock %}
    </body>
</html>
